@model string

@{
    ViewData["Title"] = "3D Модель";
}

<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh;">
    <h2>Перегляд 3D-модель</h2>

    <iframe src= "@Model"
            width="100%"
            height="500"
            frameborder="0"
            allowfullscreen>
    </iframe>

    <a href="@Url.Action("Index", "Sneakers")" class="btn btn-primary mt-4">Повернутися</a>
</div>